#include <bits/stdc++.h>

using namespace std;

int n, k, l, c, d, p, nl, np;

int main(){
 cin >> n >> k >>  l >> c >> d >> p >> nl >> np;
 int milli = k * l;
  int toast = milli/n;
  int lime = c * d;
  int toasts = p/np;
  cout << min(toast, min(lime, toasts))/n << endl;
}
